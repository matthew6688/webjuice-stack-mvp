# Handoff 2026-05-10

This handoff captures the current local release bundle before pushing it back to
`matthew6688/webjuice-stack-mvp`.

## Current State

- Branch: `main`
- Remote: `origin` -> `https://github.com/matthew6688/webjuice-stack-mvp.git`
- Repository URL: `https://github.com/matthew6688/webjuice-stack-mvp`
- Online handoff URL:
  `https://github.com/matthew6688/webjuice-stack-mvp/blob/main/docs/HANDOFF_2026-05-10.md`
- Base before this bundle: `27d0877 Restore admin leads poster board style`
- Release commit: `8ca6396 Add lead operations handoff bundle`
- Local git state after push: clean tracked worktree, `main` aligned with
  `origin/main`
- Scope: lead discovery, admin operations dashboard, Open Design handoff, template
  library, queue actions, website audit artifacts, and supporting SOP updates.

## What Changed

### Lead discovery and lead ops

- Added the low-cost Google Maps scraper discovery path:
  - scrape/import runs
  - central discovery store
  - entity dedupe
  - queue/action grouping
  - cheap website audit gate
  - selected enrichment planning and approval state
- Added supporting lead modules for:
  - discovery storage
  - maps scraper normalization
  - site audit
  - enrichment gates
  - copy briefs
  - template matching
  - website build handoff
  - Open Design handoff
- Added test coverage for discovery store promotion, second-stage discovery,
  queue actions, manual triage, mockup handoff, document model comparison, and
  site audit.

### Admin workspace

- Expanded `/admin` into a more complete operations cockpit:
  - overview
  - leads
  - queue
  - reports
  - templates
  - finance
  - intakes
  - settings
- `/admin/settings` now reads local env files plus runtime env, shows only masked
  values and setup guidance, and avoids exposing raw secrets in the page.
- `/admin/leads` now surfaces richer CRM snapshots, audit evidence, contact
  paths, lead pipeline state, and mockup/outreach readiness.
- `/admin/queue` now groups operational work around lead stage, queue action,
  outreach state, and project state.

### Open Design and template handoff

- Added/fleshed out Open Design runner support:
  - artifact fallback handling
  - app sync proof
  - validation
  - production handoff generation
  - template handoff path
- Added generated Open Design/client artifacts for active smoke runs and sample
  restaurant/roofing workflows.
- Added template library assets, public admin artifacts, template-lab scripts,
  and starter template experiment outputs.

### Discord, queue, and workflow automation

- Added lead queue action workflow:
  - `.github/workflows/run-lead-queue-action.yml`
  - `functions/admin/lead-queue-action.ts`
  - `scripts/leads/run-queue-action.js`
  - admin entrypoint tests
- Added Discord task routing/sync scripts for website tasks, template library
  setup, and lead-ops thread sync.

### Documentation

- Updated the main `HANDOFF.md`.
- Synchronized current operating docs across the 5/9-5/10 release bundle:
  - `docs/P0_P1_TODO.md`
  - `docs/MODULE_STATUS.md`
  - `docs/CORE_BUSINESS_FLOW_SOP.md`
  - `docs/LEAD_OPS_SYSTEM.md`
  - `docs/OPS_DASHBOARD_PLAN.md`
  - `docs/OPEN_DESIGN_*`
  - `docs/SECURITY.md`
- Added or promoted focused docs during this release window:
  - autoresearch optimization
  - final website production v2
  - image generation automation notes
  - legacy archive
  - niche template system
  - queue/leads stage matrix

## Verification Checklist

Run these before or immediately after pulling this bundle:

```bash
npm run build
npm run admin:test-settings-index
npm run admin:test-report-index
npm run admin:test-lead-queue-action-entrypoint
npm run funnel:test-lead-registry
npm run funnel:test-lead-outreach-index
npm run leads:test-maps-scraper-discovery
npm run leads:test-discovery-store
npm run leads:test-discovery-second-stage
npm run leads:test-promote-discovery-store
npm run leads:test-discovery-to-mockup-stage
npm run leads:test-run-queue-action
npm run leads:test-template-mockup-handoff
```

If time is short, the minimum confidence pass is:

```bash
npm run build
npm run admin:test-settings-index
npm run funnel:test-lead-outreach-index
npm run leads:test-discovery-store
npm run leads:test-run-queue-action
```

## Operator Handoff Instructions

Use this section when a new operator or agent picks up the project.

### 1. Pull and confirm the release

```bash
cd /Users/matthew/Developer/google-map-website
git fetch origin
git checkout main
git pull --ff-only origin main
git rev-parse --short HEAD
git status --short --branch
```

Expected:

- Repo is `https://github.com/matthew6688/webjuice-stack-mvp`.
- Online handoff is
  `https://github.com/matthew6688/webjuice-stack-mvp/blob/main/docs/HANDOFF_2026-05-10.md`.
- `HEAD` is `be20a74` or newer.
- `git status` shows `## main...origin/main` with no tracked file changes.
- Ignored local runtime files may still exist, especially `.env.local`, `dist/`,
  `node_modules/`, `.astro/`, `.wrangler/`, local Python venvs, and raw scraper
  dumps.

### 2. Read these docs first

Read in this order:

1. `docs/HANDOFF_2026-05-10.md` for this release bundle.
2. `docs/P0_P1_TODO.md` for the current priority board.
3. `docs/MODULE_STATUS.md` for system capability status.
4. `docs/LEAD_OPS_SYSTEM.md` for lead pipeline behavior.
5. `docs/OPS_DASHBOARD_PLAN.md` for admin dashboard direction.
6. `docs/QUEUE_LEADS_STAGE_MATRIX.md` for queue/stage semantics.
7. `docs/SECURITY.md` before touching env, tokens, deploys, or generated
   artifacts.

### 3. Do the minimum health check

```bash
npm run build
npm run admin:test-settings-index
npm run funnel:test-lead-outreach-index
npm run leads:test-discovery-store
npm run leads:test-run-queue-action
```

If one of these fails, stop and fix that first. Do not run live scraping,
enrichment, email sending, deploy, or queue workflows until this minimum set is
green.

### 4. Start the local admin workspace

```bash
npm run dev
```

Then open:

- `/admin` for the operations overview.
- `/admin/leads` for discovery and CRM snapshots.
- `/admin/queue` for next actions.
- `/admin/templates` for template inventory.
- `/admin/settings` for masked environment readiness.

The settings page is read-only for secrets. If a variable is missing, update
`.env.local` or the deployment environment, then restart the dev server or
redeploy.

### 5. Continue work from the current priority board

Current recommended sequence:

1. Finish real mockup artifact replacement for `mockup_building -> mockup_ready`.
2. Wire `draft_ready -> outreach_sent -> follow_up_due/replied -> paid_handoff`.
3. Decide what generated evidence should stay in git versus move to external
   artifact storage.
4. Keep updating `docs/P0_P1_TODO.md` when a task changes status.

### 6. Artifact and data rules

- Commit compact summaries, manifests, QA screenshots, public admin artifacts,
  and handoff evidence when they explain a decision or support an operator flow.
- Do not commit raw full Maps scraper dumps such as `results.full.json`.
- Do not commit `.env`, `.env.local`, `.env.production`, raw API responses with
  secrets, or private customer credentials.
- If a generated artifact is very large or only useful for temporary debugging,
  keep it ignored or move it to external artifact storage.

### 7. Before the next push

Run:

```bash
git status --short --branch
npm run build
npm run admin:test-settings-index
npm run funnel:test-lead-outreach-index
npm run leads:test-run-queue-action
```

Also scan for accidental secrets before staging broad generated output:

```bash
rg -n --hidden --glob '!node_modules/**' --glob '!.git/**' --glob '!dist/**' \
  '(sk-[A-Za-z0-9_-]{20,}|AIza[0-9A-Za-z_-]{20,}|gh[pousr]_[A-Za-z0-9_]{20,}|xox[baprs]-[0-9A-Za-z-]{20,})' .
```

Expected findings should be placeholders, docs examples, or already-known
captured public HTML. Sanitize anything that is a real private token.

## Important Data Notes

- This bundle contains many generated artifacts under `clients/`, `data/`,
  `public/admin-artifacts/`, and `templates/`.
- No single file was observed above GitHub's 100 MB file limit during handoff
  preparation, but the full artifact set is large.
- The largest directory is `data/`; most of that weight is generated QA and
  template experiment evidence.
- Raw Maps scraper full dumps such as `results.full.json` are intentionally
  ignored because they can include review payloads; commit compact summaries,
  queues, reports, and hard evidence instead.
- `.env`, `.env.local`, and `.env.production` remain ignored. The settings page
  should show masked configuration only.

## Known Follow-Ups

- Replace placeholder mockup artifacts with real Open Design or template runner
  outputs for the `mockup_building -> mockup_ready` path.
- Finish `draft_ready -> outreach_sent -> follow_up_due/replied -> paid_handoff`
  stage transitions and tests.
- Decide whether long-running generated experiment evidence should stay in git or
  move to external artifact storage before the repository grows much further.
- Keep `docs/P0_P1_TODO.md` as the current source of truth for next priorities.
