---
import '../styles/global.css';
import { siteConfig } from '../config/site';

interface Props {
  title: string;
  description?: string;
  active?: 'overview' | 'intakes' | 'finance' | 'queue' | 'leads' | 'templates' | 'reports' | 'settings' | 'scoring';
}

const {
  title,
  description = 'Internal admin dashboard for ProfitsLocal ops.',
  active = 'overview',
} = Astro.props;

const adminNav = [
  { key: 'overview', label: '总览', href: '/admin' },
  { key: 'leads', label: '线索', href: '/admin/leads' },
  { key: 'templates', label: '模板', href: '/admin/templates' },
  { key: 'reports', label: '报告', href: '/admin/reports' },
  { key: 'intakes', label: '项目', href: '/admin/intakes' },
  { key: 'finance', label: '财务', href: '/admin/finance' },
  { key: 'queue', label: '队列', href: '/admin/queue' },
  { key: 'scoring', label: '算法', href: '/admin/scoring' },
  { key: 'settings', label: '设置', href: '/admin/settings' },
];
---

<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <link rel="icon" href="/brand/logo-mark.svg" type="image/svg+xml" />
  <title>{title}</title>
  <script src="/funnel-context.js" defer></script>
</head>
<body>
  <div class="page-shell admin-page-shell">
    <header class="topbar admin-topbar">
      <nav class="admin-nav" aria-label="Admin navigation">
        <a class="admin-brand" href="/admin" aria-label={`${siteConfig.name} admin`}>
          <img src="/brand/logo-horizontal.svg" alt="profitslocal" />
        </a>
        <div class="admin-nav-links">
          {adminNav.map((link) => (
            <a href={link.href} class:list={{ 'is-active': link.key === active }}>{link.label}</a>
          ))}
        </div>
      </nav>
    </header>

    <main>
      <slot />
    </main>
  </div>
</body>
</html>
